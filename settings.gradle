rootProject.name = "zip-spinnaker-deployboard"

include "zip-deployboard-gate", "zip-deployboard-orca"

def setBuildFile(project) {
  project.buildFileName = "${project.name}.gradle"
  project.children.each {
    setBuildFile(it)
  }
}

rootProject.children.each {
  setBuildFile(it)
}
